三次握手：
第一次：发送端发送报文给目标，确认自身的发送能力以及目标的接受能力
第二次：目标接收到报文后，回报文给发送端，确认发送端的接受能力与自身的发送能力
第三次：发送端发送报文，目标收到报文后确认双方状态都正常，准备链接

为什么需要三次握手
三次握手的核心目的是确认双方的收发能力都正常。具体来说：
1. 防止已失效的连接请求报文段突然又传送到了服务端，因而产生错误。
2. 双方都确认自己的发送能力和对方的接收能力正常。第一次握手确认了客户端的发送能力和服务器的接收能力；第二次握手确认了服务器的发送能力和客户端的接收能力；第三次握手最终确认了双方的收发能力都正常。
3. 初始化连接参数，如初始序列号等。
步骤	          发送方	报文	          状态变化
第一次握手	  客户端	SYN = j	      客户端 → SYN_SENT
第二次握手	  服务器	SYN = k,      ACK = j+1	服务器 → SYN_RCVD
第三次握手	  客户端	ACK = k+1	    客户端 → ESTABLISHED

查看链接状态
ss -tan
netstat -tan

四次挥手
第一次：发送端发送报文告诉目标自己没有消息要发送了，关闭了自己的通道
第二次：目标接收到报文后，发送报文给发送端告诉他自己可能还有消息要发送，保持自身链接
第三次：目标表示自己没有消息要发送了，发送消息给发送端
第四次：发送端接收到后，进入time_wait状态，发送最后一个报文给目标，等待报文送达并且等待通道里残余的报文自然消失，最后断开链接

为什么需要四次挥手
四次挥手的核心原因是 TCP 连接是全双工的，需要等待对方 FIN。具体来说：
1. TCP 连接是全双工的，客户端和服务器都可以独立地关闭自己的发送通道。当客户端发送 FIN 后，它只是关闭了自己到服务器的发送通道，但服务器可能还有数据要发送给客户端。
2. 服务器收到 FIN 后，先发送 ACK 确认（第二次挥手），然后处理完剩余数据后再发送自己的 FIN（第三次挥手）。如果服务器没有剩余数据要发送，ACK 和 FIN 可以合并在一个包中发送，理论上可以变成三次挥手，但协议规定必须分开，以确保可靠性。
3. 客户端收到服务器的 FIN 后，必须发送 ACK 确认（第四次挥手），然后进入 TIME_WAIT 状态等待 2MSL 时间，确保最后一个 ACK 能够到达服务器。



TIME_WAIT
time_wait的作用是等待最后一个报文送达刀目的地，并且等待信道内的残余报文自然消失
TIME_WAIT 状态的持续时间通常为 2MSL。在大多数操作系统中，MSL 的值通常为 30 秒到 2 分钟不等，因此 TIME_WAIT 状态通常持续 1 到 4 分钟。
在 Linux 系统中，可以通过以下命令查看和修改 MSL 值：
• 查看当前 MSL 值：cat /proc/sys/net/ipv4/tcp_fin_timeout
• 修改 MSL 值：echo "60" > /proc/sys/net/ipv4/tcp_fin_timeout（单位：秒）
