
# 四次挥手与 TIME_WAIT 详解

## 一、四次挥手过程

TCP 连接是全双工的，断开连接时需要双方各自关闭自己的发送通道，因此需要四次交互。

| 步骤 | 发送方 | 报文 | 说明 |
| :--- | :--- | :--- | :--- |
| **第一次挥手** | 客户端 | `FIN = 1` | 客户端告诉服务器："我没有数据要发送了，关闭我的发送通道" |
| **第二次挥手** | 服务器 | `ACK = 1` | 服务器确认收到，但服务器可能还有数据要发送给客户端，因此保持接收通道开放 |
| **第三次挥手** | 服务器 | `FIN = 1` | 服务器处理完剩余数据后，告诉客户端："我也没有数据要发送了，关闭我的发送通道" |
| **第四次挥手** | 客户端 | `ACK = 1` | 客户端确认收到，进入 TIME_WAIT 状态，等待 2MSL 后断开连接 |

---

## 二、为什么需要四次挥手？

1. **TCP 是全双工连接**
   - 客户端和服务器都可以独立地关闭自己的发送通道。
   - 客户端发送 FIN 后，只是关闭了自己到服务器的发送通道，但服务器可能还有数据要发送给客户端。

2. **服务器需要时间处理剩余数据**
   - 服务器收到 FIN 后，先发送 ACK 确认（第二次挥手），然后处理完剩余数据后再发送自己的 FIN（第三次挥手）。
   - 如果服务器没有剩余数据要发送，ACK 和 FIN 可以合并在一个包中发送，理论上可以变成三次挥手，但协议规定必须分开，以确保可靠性。

3. **确保最后一个 ACK 能够到达服务器**
   - 客户端收到服务器的 FIN 后，必须发送 ACK 确认（第四次挥手），然后进入 TIME_WAIT 状态等待 2MSL 时间，确保最后一个 ACK 能够到达服务器。

---

## 三、TIME_WAIT 状态详解

### 作用

1. **等待最后一个 ACK 报文送达目的地**
   - 如果最后一个 ACK 丢失，服务器会重传 FIN，客户端需要能够重新发送 ACK。

2. **等待信道内的残余报文自然消失**
   - 确保本次连接中的所有报文都从网络中消失，避免影响后续新建的连接。

### 持续时间

TIME_WAIT 状态的持续时间通常为 **2MSL**（Maximum Segment Lifetime，最大报文段生存时间）。

| 系统 | 默认 MSL 值 | TIME_WAIT 持续时间 |
| :--- | :--- | :--- |
| 大多数操作系统 | 30 秒 ~ 2 分钟 | 1 ~ 4 分钟 |
| Linux | 可配置 | 2 × tcp_fin_timeout |

### Linux 中查看和修改 MSL 值

```bash
# 查看当前 MSL 值（单位：秒）
cat /proc/sys/net/ipv4/tcp_fin_timeout

# 修改 MSL 值（例如设为 60 秒）
echo "60" > /proc/sys/net/ipv4/tcp_fin_timeout


---


