# TCP 连接管理：三次握手与四次挥手

## 一、三次握手（建立连接）

### 握手过程

| 步骤 | 发送方 | 报文内容 | 状态变化 | 确认内容 |
| :--- | :--- | :--- | :--- | :--- |
| **第一次握手** | 客户端 | `SYN = j` | 客户端 → `SYN_SENT` | 客户端确认自身的**发送能力**，服务器确认自身的**接收能力** |
| **第二次握手** | 服务器 | `SYN = k, ACK = j+1` | 服务器 → `SYN_RCVD` | 服务器确认自身的**发送能力**，客户端确认自身的**接收能力** |
| **第三次握手** | 客户端 | `ACK = k+1` | 客户端 → `ESTABLISHED` | 双方最终确认**收发能力都正常**，连接正式建立 |

### 为什么需要三次握手？

1. **确认双方的收发能力都正常**
   - 第一次握手：确认客户端的发送能力 + 服务器的接收能力
   - 第二次握手：确认服务器的发送能力 + 客户端的接收能力
   - 第三次握手：最终确认双方的收发能力都正常

2. **防止已失效的连接请求报文段突然传送到服务器，产生错误**
   - 如果没有第三次握手，客户端之前发送的延迟 SYN 报文到达服务器后，服务器会误认为客户端要建立连接，从而浪费资源。

3. **初始化连接参数**
   - 双方协商初始序列号（ISN），为后续数据传输的可靠性和有序性奠定基础。

### 查看连接状态

```bash
ss -tan
netstat -tan
