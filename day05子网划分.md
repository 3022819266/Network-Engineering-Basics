# 子网划分计算示例

## 题目

已知 IP 地址为 `192.168.3.12`，子网掩码为 `255.255.255.224`，求其网络地址、广播地址、可分配主机数量、主机 IP 范围。

---

## 解题步骤

### 第一步：判断地址类别

`192.168.3.12` 属于 **C 类地址**，默认网络号为前 24 位（前三字节），主机号为后 8 位（最后一字节）。

### 第二步：分析子网掩码

将子网掩码的最后一个字节转换为二进制：
224 = 11100000


由此可知，主机号部分**借了 3 位**作为子网号（即掩码中为 1 的位数），剩余 **5 位**作为主机号（即掩码中为 0 的位数）。

### 第三步：计算可分配主机数量

主机号有 5 位，因此每个子网可容纳的主机数为：
2⁵ - 2 = 32 - 2 = 30


> **为什么要减 2？** 因为每个子网中，**网络地址**（主机位全为 0）和**广播地址**（主机位全为 1）不能分配给主机使用。

### 第四步：求网络地址

网络地址 = IP 地址 **AND** 子网掩码（按位与运算）

将 IP 地址的最后一个字节转换为二进制：
12 = 00001100


按位与运算：
00001100 （IP: 12）
& 11100000 （掩码: 224）
00000000 （结果: 0）


因此，网络地址为 **`192.168.3.0`**。

### 第五步：求广播地址

广播地址 = 网络地址的主机位全部置为 1

网络地址最后一字节为 `00000000`，后 5 位为主机位，全部置 1：
00011111 = 31


因此，广播地址为 **`192.168.3.31`**。

### 第六步：确定主机 IP 范围

主机 IP 范围 = 网络地址 + 1 ~ 广播地址 - 1
192.168.3.1 ~ 192.168.3.30


---

## 最终结果汇总

| 项目 | 结果 |
| :--- | :--- |
| **网络地址** | `192.168.3.0` |
| **广播地址** | `192.168.3.31` |
| **可分配主机数量** | 30 台 |
| **主机 IP 范围** | `192.168.3.1` ~ `192.168.3.30` |



