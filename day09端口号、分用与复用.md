
# 运输层：端口号、复用与分用

> 本笔记梳理 TCP/IP 体系运输层的核心概念：为什么需要统一标识、端口号的定义与分类、发送方复用与接收方分用的全过程，以及应用层常用协议对应的熟知端口号。

---

## 一、为什么需要统一标识？

- 运行在计算机上的进程通常使用 **进程标识符（PID）** 来标志。
- 但因特网上的计算机运行不同的操作系统（Windows、Linux、Mac OS），它们使用 **不同格式** 的进程标识符。
- 为了实现跨操作系统的网络通信，TCP/IP 体系必须使用统一的方法来标识应用进程。

---

## 二、什么是端口号？

- TCP/IP 体系的运输层使用 **端口号** 来区分应用层的不同应用进程。
- 端口号使用 **16 比特** 表示，取值范围是 `0 ~ 65535`。

---

## 三、端口号的三类划分

| 类别 | 取值范围 | 说明 | 典型示例 |
| --- | --- | --- | --- |
| **熟知端口号** | `0 ~ 1023` | 由 IANA 指派给 TCP/IP 体系中最重要的应用协议 | FTP 使用 21/20，HTTP 使用 80，DNS 使用 53 |
| **登记端口号** | `1024 ~ 49151` | 供没有熟知端口号的应用程序使用，必须在 IANA 按规定手续登记以防止重复 | Microsoft RDP（远程桌面）使用 3389 |
| **短暂端口号** | `49152 ~ 65535` | 留给客户进程暂时使用（动态端口号）。服务器收到报文后通过该端口号回复客户，通信结束后释放供其他进程使用 | 客户端临时分配的端口 |

---

## 四、发送方与接收方的复用与分用

### 1. 发送方的复用过程

**复用（Multiplexing）** 是指在发送方，多个不同的应用进程将数据汇聚，最终交由网络层发送的过程。

1. **应用层产生数据**：多个应用进程生成各自的应用报文。
2. **运输层复用**：应用报文到达运输层。
   - 若使用 UDP 协议，则进行 **UDP 复用**；
   - 若使用 TCP 协议，则进行 **TCP 复用**。
   - 在此过程中，系统会加上 **端口号**。UDP 复用后形成 **用户数据报**，TCP 复用后形成 **报文段**。
3. **网络层复用**：运输层的数据包继续向下传递给网络层。
   - 网络层进行 **IP 复用**，根据运输层使用的协议打上相应标识（UDP 对应协议字段 = 17，TCP 对应协议字段 = 6）。
4. **网络传输**：最终封装成 **IP 数据报**，发送到网络中进行传输。

### 2. 接收方的分用过程

**分用（Demultiplexing）** 是指在接收方，网络层收到的数据被逐层拆解，最终准确交付给目标应用进程的过程。

1. **网络层分用**：接收方收到 IP 数据报后，网络层首先进行 **IP 分用**。
   - 根据 IP 数据报中的协议字段判断：协议字段为 17 交给 UDP 处理，为 6 交给 TCP 处理。
2. **运输层分用**：
   - 若是 UDP 数据，进行 **UDP 分用**，得到用户数据报；
   - 若是 TCP 数据，进行 **TCP 分用**，得到报文段。
   - 在此过程中，通过 **端口号** 来识别目标应用进程。
3. **应用层接收**：运输层将剥离头部信息后的应用报文，根据端口号准确交付给对应的应用进程。

### 3. 数据流向示意

```text
发送方（自上而下，复用）
应用进程 → 应用报文
        ↓  运输层：加端口号（UDP 复用 / TCP 复用）
   用户数据报 / 报文段
        ↓  网络层：IP 复用（协议字段 = 17 / 6）
       IP 数据报  →  发送到网络

接收方（自下而上，分用）
       IP 数据报
        ↓  网络层：IP 分用（看协议字段 17 / 6）
   用户数据报 / 报文段
        ↓  运输层：分用（看端口号）
   应用报文  →  交付对应应用进程
```

---

## 五、应用层常用协议对应的熟知端口号

### 1. 使用 UDP 协议的应用

| 应用协议 | 端口号 | 备注 |
| --- | --- | --- |
| RIP | 520 | 路由信息协议 |
| DNS | 53 | 域名系统 |
| TFTP | 69 | trivial 文件传输协议 |
| SNMP | 161 | 简单网络管理协议 |
| DHCP | 67 / 68 | 67 服务器端，68 客户端 |

### 2. 使用 TCP 协议的应用

| 应用协议 | 端口号 | 备注 |
| --- | --- | --- |
| SMTP | 25 | 简单邮件传输协议 |
| FTP | 21 / 20 | 21 控制连接，20 数据连接 |
| BGP | 179 | 边界网关协议 |
| HTTP | 80 | 超文本传输协议 |
| HTTPS | 443 | 安全超文本传输协议 |

---

## 六、核心关系总结

- 应用层协议通过 **熟知端口号** 与运输层的 **UDP** 或 **TCP** 协议相关联。
- **端口号** 是跨操作系统统一标识应用进程的关键手段。
- **复用** 让发送方多个进程的数据汇聚下发，**分用** 让接收方按协议字段与端口号逐层准确交付，二者共同实现运输层「区分进程、汇聚与分发数据」的核心功能。
