MAC地址是以太网的MAC子层所使用的地址，属于数据链路层的范畴，IP地址是TCP/IP体系结构中网际层所使用的地址，ARP协议属于TCP/IP体系结构的网际层，其作用是已知设备的分配到的
IP地址，使用ARP协议可以通过IP地址获取到设备的MAC地址，IP地址与ARP属于网际层的范畴

MAC地址
它并不属于物理层而是属于数据链路层
IEEE 802局域网的MAC地址格式由48bit构成，每八个bit为一位，共六位，前三个字节为组织唯一标识符，OUI，由IEEE的注册管理机构分配，后三位为网络接口标识符，由获得OUI的厂商
自行随意分配，因此MAC地址又称EUI-48标识符

在MAC地址中每个字节由b0~b7共八位组成，其中第一字节的b1为0表示该MAC地址类型为全球管理，为1表示为本地管理，其b0为为0表示单播地址，为1表示多播地址
b7 b6 b5 b4 b3 b2 b1 b0
由此,MAC地址可以分为四类：
全球管理单播地址
厂商生产网络设备时固化

全球管理多播地址
标准网络设备所支持的多播地址，用于特定类型

本地管理单播地址
由网络管理员分配，覆盖网络接口的全球管理单播地址

本地管理多播地址
用户对主机进行软件分配，以此表明其属于那些多播组，当剩余46位全为1时，即FF-FF-FF-FF-FF-FF,表示为广播地址

MAC地址的发送顺序:
字节发送顺序：1-6
字节内比特发送顺序：b0-b7

ip地址是因特网上的主机和路由器所使用的地址，用于标识两部分信息
网络编号：标识因特网上数以万计的网络
主机编号：标识同一网络上不同主机或路由器接口

数据包转发过程中，源IP地址和目的ip地址不变，源mac地址和目的mac地址逐个链路改变

ARP地址解析协议
查找MAC地址：源主机在自己的ARP高速缓存表中查找目的主机的IP地址所对应的MAC地址，若找到了，则可以封装MAC帧进行发送；若找不到，则发送ARP请求（封装在广播MAC帧中）；

接收请求与响应：目的主机收到ARP请求后，将源主机的IP地址与MAC地址记录到自己的ARP高速缓存表中，然后给源主机发送ARP响应（封装在单播MAC帧中），ARP响应中包含有目的主机的IP地址和MAC地址；

接收响应：源主机收到ARP响应后，将目的主机的IP地址与MAC地址记录到自己的ARP高速缓存表中，然后就可以封装之前想发送的MAC帧并发送给目的主机；

作用范围：逐段链路或逐个网络使用；

其他类型报文：除ARP请求和响应外，ARP还有其他类型的报文（例如用于检查IP地址冲突的无敌ARP、免费ARP(Gratuitous ARP)）；

安全问题：ARP没有安全验证机制，存在ARP欺骗（攻击）问题。









