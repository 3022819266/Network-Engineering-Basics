# 数据链路层与网际层核心概念

## 地址体系概览

| 地址类型 | 所属层次 | 作用 |
| :--- | :--- | :--- |
| **MAC 地址** | 数据链路层（MAC 子层） | 标识同一链路上的设备接口 |
| **IP 地址** | 网际层（TCP/IP 体系） | 标识因特网上的网络与主机 |
| **ARP 协议** | 网际层（TCP/IP 体系） | 已知 IP 地址，获取对应的 MAC 地址 |

> **关键区分**：MAC 地址不属于物理层，而是属于**数据链路层**的范畴。

---

## MAC 地址详解

### 地址格式
IEEE 802 局域网的 MAC 地址由 **48 bit**（6 字节）构成，又称 **EUI-48 标识符**。

| 组成部分 | 长度 | 分配方式 |
| :--- | :--- | :--- |
| **OUI（组织唯一标识符）** | 前 3 字节（24 bit） | 由 IEEE 注册管理机构统一分配 |
| **网络接口标识符** | 后 3 字节（24 bit） | 由获得 OUI 的厂商自行分配 |

### 地址分类（基于首字节的 b1 和 b0 位）

每个字节由 b7 ~ b0 共 8 位组成，其中**第一字节**的以下两位决定地址类型：

| 位 | 含义 | 取值说明 |
| :--- | :--- | :--- |
| **b1** | 管理方式 | 0 = 全球管理（IEEE 分配）；1 = 本地管理（管理员分配） |
| **b0** | 传输类型 | 0 = 单播地址；1 = 多播地址 |

由此，MAC 地址可分为四类：

| 类型 | 说明 |
| :--- | :--- |
| **全球管理单播地址** | 厂商生产网络设备时固化在硬件中，全球唯一 |
| **全球管理多播地址** | 标准网络设备所支持的多播地址，用于特定类型通信 |
| **本地管理单播地址** | 由网络管理员手动分配，覆盖网络接口的全球管理单播地址 |
| **本地管理多播地址** | 用户通过软件分配，表明主机属于哪些多播组 |

> **广播地址**：当剩余 46 位全为 1 时，即 `FF-FF-FF-FF-FF-FF`，表示广播地址。

### 发送顺序
- **字节发送顺序**：从第 1 字节到第 6 字节
- **字节内比特发送顺序**：从 b0 到 b7

---

## IP 地址

IP 地址是因特网上的主机和路由器所使用的地址，用于标识两部分信息：

| 组成部分 | 作用 |
| :--- | :--- |
| **网络编号** | 标识因特网上数以万计的网络 |
| **主机编号** | 标识同一网络上不同的主机或路由器接口 |

> **关键特性**：在数据包转发过程中，**源 IP 地址和目的 IP 地址始终保持不变**，而**源 MAC 地址和目的 MAC 地址会逐个链路改变**。

---

## ARP 地址解析协议

### 工作流程

#### 1. 查找 MAC 地址
源主机先在自身的 **ARP 高速缓存表**中查找目的主机的 IP 地址所对应的 MAC 地址：
- **找到了** → 直接封装 MAC 帧进行发送
- **找不到** → 发送 ARP 请求（封装在**广播 MAC 帧**中）

#### 2. 接收请求与响应
目的主机收到 ARP 请求后：
1. 将源主机的 IP 地址与 MAC 地址记录到自己的 ARP 高速缓存表中
2. 向源主机发送 **ARP 响应**（封装在**单播 MAC 帧**中），响应中包含目的主机的 IP 地址和 MAC 地址

#### 3. 接收响应
源主机收到 ARP 响应后：
1. 将目的主机的 IP 地址与 MAC 地址记录到自己的 ARP 高速缓存表中
2. 封装之前想发送的 MAC 帧，发送给目的主机

### 其他特性

| 特性 | 说明 |
| :--- | :--- |
| **作用范围** | 逐段链路或逐个网络使用 |
| **其他报文类型** | 除 ARP 请求和响应外，还包括用于检查 IP 地址冲突的**无敌 ARP**、**免费 ARP（Gratuitous ARP）**等 |
| **安全问题** | ARP 没有安全验证机制，存在 **ARP 欺骗（攻击）**问题 |








