# ICMP（网际控制报文协议）

ICMP 协议作用于**网际层**。

**目的**：为了提高 IP 数据报交付成功的机会，以及更高效地转发 IP 数据报。

**工作原理**：主机或路由器使用 ICMP 发送**差错报告报文**和**询问报文**。需要注意的是，ICMP 报文本身是被封装在 IP 数据报中进行发送的。

---

## 一、 ICMP 差错报告报文

ICMP 差错报告报文主要分为以下五种：

### 1. 终点不可达 (Destination Unreachable)
当路由器或主机不能将数据报交付到终点时，就会向源点发送**终点不可达报文**。

### 2. 源点抑制 (Source Quench)
当路由器或主机由于拥塞而被迫丢弃数据包时，会向源点发送**源点抑制报文**。其目的是通知源点降低数据发送速率，以缓解网络拥塞。

### 3. 时间超过 (Time Exceeded)
* **TTL 归零**：当路由器收到一个目的 IP 不是自己的 IP 数据报时，会将其生存时间（TTL）值减 1。若结果不为 0 则转发；若结果为 0，则丢弃该数据包，并向源点发送时间超过报文。
* **重组超时**：当终点在预先规定的时间内不能收齐一个数据报的所有分片时，会将已收到的分片全部丢弃，并向源点发送时间超过报文。

### 4. 参数问题 (Parameter Problem)
当路由器或目的主机收到 IP 数据报后，通过检查首部中的检验字段，发现数据报在传输过程中出现了误码或参数错误，就会丢弃该数据报，并向源点发送**参数问题报文**。

### 5. 改变路由/重定向 (Redirect)
路由器发现主机将数据报发给了非最佳路径的下一跳路由器时，会把**改变路由报文**发送给该主机，通知主机下一次直接将数据报发送给更合适的路由器。

### ⛔ 不发送 ICMP 报文的情况
为了防止“广播风暴”或无效反馈，以下情况**不会**发送 ICMP 差错报告报文：
1. 对 ICMP 差错报文本身，不再发送 ICMP 差错报文。
2. 对数据报的第一个分片之后的**后续所有数据报分片**，不发送 ICMP 差错报文。
3. 对具有**多播地址**（Multicast）的数据报，不发送 ICMP 差错报文。
4. 对具有**特殊地址**（如 `127.0.0.0` 网段、`0.0.0.0` 等）的数据报，不发送 ICMP 差错报文。

---

## 二、 ICMP 询问报文

常见的 ICMP 询问报文主要有以下两种：

### 1. 回送请求和回答 (Echo Request & Echo Reply)
由主机或路由器向目的主机发送。收到此报文的主机必须向源主机或路由器发回回答报文。
* **目的**：测试目的地的可达性并了解相关信息（常用于 **`PING`** 命令）。

### 2. 时间戳请求和回答 (Timestamp Request & Reply)
请求某台主机或路由器回答当前的日期和时间。报文中包含一个 32 位的字段，写入的整数代表从 1900 年 1 月 1 日起到当前时刻的总秒数。
* **目的**：用于系统的时间同步和测量网络时延。

---

## 三、 ICMP 的典型应用

* **PING**（测试网络连通性与延迟）
* **TRACEROUTE**（路由追踪，查看数据包经过的路径）











