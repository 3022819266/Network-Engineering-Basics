# 计算机网络性能指标

## 速率 (Rate)
指计算机网络上的主机在数字信道上传送比特流的速率，也称**比特率**或**数据率**，单位为 bit/s。

### 1. 数据存储单位（容量）
> **换算规则**：按 2¹⁰ (1024) 进制换算

| 单位 | 换算公式 | 等于 2 的幂次 |
| :--- | :--- | :--- |
| 1 KB | 2¹⁰ B | 2¹⁰ |
| 1 MB | 2¹⁰ × 2¹⁰ B | 2²⁰ |
| 1 GB | 2¹⁰ × 2²⁰ B | 2³⁰ |
| 1 TB | 2¹⁰ × 2³⁰ B | 2⁴⁰ |

### 2. 数据传输速率（网速）
> **换算规则**：按 10³ (1000) 进制换算

| 单位 | 换算公式 | 等于 10 的幂次 |
| :--- | :--- | :--- |
| 1 kb/s | 10³ b/s | 10³ |
| 1 Mb/s | 10³ × 10³ b/s | 10⁶ |
| 1 Gb/s | 10³ × 10⁶ b/s | 10⁹ |
| 1 Tb/s | 10³ × 10⁹ b/s | 10¹² |

#### 计算示例
> **问题**：100MB 的数据，在 100Mb/s 的速率下，要多长时间才能发送完毕？

**计算过程**：
1. 统一单位（将 MB 转换为 bit）：100 MB = 100 × 2²⁰ B = 100 × 2²⁰ × 8 bit
2. 统一速率单位（将 Mb/s 转换为 b/s）：100 Mb/s = 100 × 10⁶ b/s
3. 计算时间：
   时间 = (100 × 2²⁰ × 8) ÷ (100 × 10⁶) ≈ 8.38 s

---

## 带宽 (Bandwidth)
- **定义**：信号所包含的各种不同频率成分所占据的频率范围，用来表示网络的通信线路所能传送数据的能力。
- **网络带宽含义**：表示在单位时间内从网络中的某一点到另一点所能通过的**最高数据率**。
- **单位**：Hz, kHz, MHz, GHz

## 吞吐量 (Throughput)
在单位时间内通过某个网络（或信道、接口）的数据量。

## 时延 (Delay)
数据（分组）从网络（或链路）的一端传送到另一端所需的时间。由以下三部分构成：

| 时延类型 | 计算公式 | 说明 |
| :--- | :--- | :--- |
| **发送时延** | 分组长度 (bit) ÷ 发送速率 (bit/s) | 主机或路由器发送数据帧所需要的时间 |
| **传播时延** | 信道长度 (m) ÷ 电磁波传播速率 (m/s) | 电磁波在信道中传播一定的距离需要花费的时间 |
| **处理时延** | - | 主机或路由器在收到分组时进行处理（如分析首部、提取数据、差错检验等）所花费的时间，通常难以精确计算 |

> **电磁波在不同介质中的传播速率**：
> - 自由空间：3 × 10⁸ m/s
> - 铜线：2.3 × 10⁸ m/s
> - 光纤：2 × 10⁸ m/s

> **注意**：在实际问题中，应通过具体计算来判断是由**发送时延**主导还是**传播时延**主导。

## 时延带宽积 (Delay-Bandwidth Product)
- **公式**：传播时延 × 带宽
- **物理意义**：若发送端连续发送数据，则在所发送的第一个比特即将到达终点时，发送端就已经发送了“时延带宽积”个比特。
- **别称**：以比特为单位的链路长度。

## 往返时间 (RTT)
从源主机发送分组开始，直到源主机收到来自目的主机的确认分组为止所需的时间。影响它的主要因素是**传播时延**。

## 利用率 (Utilization)
分为信道利用率和网络利用率。
- **规律**：当信道的利用率增大时，其引起的时延也会迅速增加，因此信道利用率并非越高越好。
- **计算公式**：
  D = D₀ ÷ (1 - U)
  - D₀：表示网络空闲时的时延
  - D：表示网络当前的时延
  - U：表示利用率

## 丢包率 (Packet Loss Rate)
即分组丢失率，指在一定的时间范围内，传输过程中丢失的分组数量与总分组数量的比率。可具体分为接口丢包率、节点丢包率、链路层丢包率、路径丢包率、网络丢包率等。

### 分组丢失的两种情况
1. 在传输过程中出现误码（错误码）。
2. 到达一台队列已满的分组交换机时被丢弃。

> **意义**：丢包率直接反映了网络的拥塞情况。





