# 一、 虚拟专用网 VPN (Virtual Private Network)

**基本定义**：利用公用的因特网作为本机构各专用网之间的通信载体，这样的专用网又称为虚拟专用网。

**VPN的分类**：
* **内联网 VPN**：同一机构内不同部门的内部网络所构成的虚拟专用网。
* **外联网 VPN**：一个机构的VPN需要有某些外部机构（通常就是合作伙伴）参加进来时。
* **远程接入 VPN**：在外地工作的员工需要访问公司内部专用网络时，在任何地点接入因特网，运行驻留在员工PC中的VPN软件，在员工的PC和公司的主机之间建立VPN隧道，即可访问专用网络中的资源。

**安全保障**：VPN要保证传输数据的安全性，会将原始的内部数据报进行加密，然后再将其封装成为在因特网上发送到的外部数据报。

<br>

# 二、 网络地址转换 NAT (Network Address Translation)

**产生背景**：由于 IP 地址的紧缺，机构申请到的 IP 地址数量往往远小于本机构所拥有的主机数量。因此，VPN 中的各主机所分配的地址应该是本机构可自由分配的专用地址，而不是需要申请的、在因特网上使用的公有地址。

**IPv4 缓解现状**：虽然采用了无分类编址方式，但由于用户数目激增（特别是小型办公室和家庭网络），IPv4 地址空间即将面临耗尽的危险仍未解除。

**NAT 的作用**：1994年提出的 NAT 方法再次缓解了 IP 地址空间耗尽的问题。它能使大量使用内部专用地址的专用网络用户共享少量外部全球地址来访问因特网上的主机和资源。

**NAPT (网络地址与端口号转换)**：
由于绝大多数网络应用使用 TCP 或 UDP 传送数据，因此可以利用运输层的端口号和 IP 地址一起进行转换。这种将端口号和 IP 地址一起进行转换的技术叫作网络地址与端口号转换 NAPT。
* **效果**：用一个全球 IP 地址就可以使多个拥有本地地址的主机同时和因特网上的主机进行通信。

**P2P 应用的 NAT 问题**：对于一些 P2P 网络应用，需要外网主机主动与内网主机进行通信，在通过 NAT 时会遇到问题，需要网络应用自己使用一些特殊的 NAT穿越技术 来解决问题。

**安全性**：由于 NAT 对外网屏蔽了内网主机的网络地址，能为内网的主机提供一定的安全保护。
