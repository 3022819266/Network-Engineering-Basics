速率：
指计算机网络上的主机在数字信道上传送比特流的速率，也称比特率或数据率，写为bit/s
1. 数据存储单位（容量）
> 规则：按 2^10 (1024) 进制换算

| 单位 | 换算公式 | 等于 2 的幂次 |
| :--- | :--- | :--- |
| 1 KB | 2^10 B | 2^10 |
| 1 MB | K * KB (2^10 * 2^10 B) | 2^20 |
| 1 GB | K * MB (2^10 * 2^20 B) | 2^30 |
| 1 TB | K * GB (2^10 * 2^30 B) | 2^40 |

---

### 2. 数据传输速率（网速）
> 规则：按 10^3 (1000) 进制换算

| 单位 | 换算公式 | 等于 10 的幂次 |
| :--- | :--- | :--- |
| 1 kb/s | 10^3 b/s | 10^3 |
| 1 Mb/s | k * kb/s (10^3 * 10^3 b/s) | 10^6 |
| 1 Gb/s | k * Mb/s (10^3 * 10^6 b/s) | 10^9 |
| 1 Tb/s | k * Gb/s (10^3 * 10^9 b/s) | 10^12 |
举一个例子：
100MB速率为100mb/s,要多长时间才能发送完毕？
100MB/100MBPS=2^20B/10^6B/S=2^20/10^6S=8.38S

带宽：
信号所包含的各种不同频率成分所占据的频率范围，用来表示网络的通信线路所能传送数据的能力，网路带宽表示在单位时间内从网络中的某一点到另一点所能通过的“最高数据率”
单位：HZ KHZ MHZ GHZ

吞吐量：
在单位时间内通过某个网络的数据量

时延：
由发送时延，传播时延，处理时延构成

发送时延：
分组长度（b）/发送速率（b/s）

传播时延：
信道长度（m）/电磁波传播速率（m/s）
自由空间:3*10^8m/s
铜线:2.3*10^8m/s
光纤：2*10^8m/s

处理时延：
由于各种原因，很难计算

在实际问题中，应通过具体计算来判断是由发送时延主导还是传播时延主导

时延带宽积：
即传播时延*带宽
若发送端连续发送数据，则在所发送的第一个比特即将到达终点时，发送端就把已经发送了时延带宽积个比特，链路的时延带宽积又称为以比特为单位的链路长度

往返时间 RTT
从源主机发送分组开始，知道源主机收到来自目的主机的确认分组为止所需的时间，影响它的主要是传播时延

利用率：
信道利用率，网络利用率
当信道的利用率增大时，其引起的时延也会迅速增加，因此信道利用率并非越高越好
D0 表示网络空闲的时延 D 表示网络当前的时延 U 表示利用率
D=D0/1-U

丢包率：
即分组丢失率，指在一定的时间范围内，传输过程中丢失的分组数量与总分组数量的比率，可具体分为接口丢包率，节点丢包率，链路层丢包率，路径丢包率，网络丢包率等
分组丢失的两种情况：
1.在传输过程中出现错误码
2.到达一台队列已满的分组交换机时被丢弃
丢包率反应了网络的拥塞情况












