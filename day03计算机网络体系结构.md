# 网络体系结构基础

## 常见网络体系结构

| 体系结构 | 地位 | 说明 |
| :--- | :--- | :--- |
| **OSI 参考模型** | 法律上的标准 | 由国际标准化组织（ISO）制定，理论完善但实际应用较少 |
| **TCP/IP 模型** | 事实上的国际标准 | 起源于 ARPANET，被互联网广泛采用，是实际部署的事实标准 |

---

## 三个核心概念

### 1. 实体 (Entity)
任何可以发送或接收消息的**硬件或软件进程**。

### 2. 协议 (Protocol)
控制两个**对等实体**进行逻辑通信的**规则集合**。

#### 协议的三要素

| 要素 | 定义 |
| :--- | :--- |
| **语法** | 定义交换信息的**格式**（数据与控制信息的结构） |
| **语义** | 定义双方所要完成的**操作**（发出何种控制信息、完成何种动作、做出何种响应） |
| **同步** | 定义双方的**时序关系**（事件实现的顺序、速度匹配、排序） |

### 3. 服务 (Service)
下层通过层间接口向上层提供的功能支持。

> **关键区分**：
> - **协议是水平的**：控制对等实体之间的通信规则
> - **服务是垂直的**：下层向上层提供的功能支持

---

## 数据单元

### 协议数据单元 (PDU)
**对等层**之间传送的数据包称为该层的协议数据单元。

### 服务数据单元 (SDU)
同一系统内，**层与层之间**交换的数据包称为服务数据单元。

### PDU 与 SDU 的关系

- 多个 SDU 可以合成为一个 PDU
- 一个 SDU 可以划分为几个 PDU
- 一般情况下，下层 SDU = 上层 PDU + 本层首部（Header）
